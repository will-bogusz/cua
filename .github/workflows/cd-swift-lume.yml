name: "CD: Lume (macOS)"

on:
  push:
    tags:
      - "lume-v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to notarize (without v prefix)"
        required: true
        default: "0.1.0"
  workflow_call:
    inputs:
      version:
        description: "Version to notarize"
        required: true
        type: string
      channel:
        description: "Release channel"
        required: false
        default: stable
        type: string
      source_ref:
        description: "Exact source SHA or immutable ref to build"
        required: false
        default: ""
        type: string
      bundle_version:
        description: "Numeric Apple bundle/package version"
        required: false
        default: ""
        type: string
    secrets:
      APPLICATION_CERT_BASE64:
        required: true
      INSTALLER_CERT_BASE64:
        required: true
      CERT_PASSWORD:
        required: true
      APPLE_ID:
        required: true
      TEAM_ID:
        required: true
      APP_SPECIFIC_PASSWORD:
        required: true
      DEVELOPER_NAME:
        required: true
      PROVISIONING_PROFILE_BASE64:
        required: true

permissions:
  contents: write
  issues: read
  pull-requests: read

concurrency:
  group: cd-lume-${{ github.ref }}
  cancel-in-progress: false

env:
  APPLICATION_CERT_BASE64: ${{ secrets.APPLICATION_CERT_BASE64 }}
  INSTALLER_CERT_BASE64: ${{ secrets.INSTALLER_CERT_BASE64 }}
  CERT_PASSWORD: ${{ secrets.CERT_PASSWORD }}
  APPLE_ID: ${{ secrets.APPLE_ID }}
  TEAM_ID: ${{ secrets.TEAM_ID }}
  APP_SPECIFIC_PASSWORD: ${{ secrets.APP_SPECIFIC_PASSWORD }}
  DEVELOPER_NAME: ${{ secrets.DEVELOPER_NAME }}
  PROVISIONING_PROFILE_BASE64: ${{ secrets.PROVISIONING_PROFILE_BASE64 }}

jobs:
  notarize:
    runs-on: macos-15
    outputs:
      version: ${{ steps.set_version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.source_ref || github.ref }}

      - name: Set version
        id: set_version
        run: |
          # Determine version from tag or input
          if [[ "$GITHUB_REF" == refs/tags/lume-v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/lume-v}"
            echo "Using version from tag: $VERSION"
          elif [[ -n "${{ inputs.version }}" ]]; then
            VERSION="${{ inputs.version }}"
            echo "Using version from input: $VERSION"
          else
            echo "Error: No version found in tag or input"
            exit 1
          fi

          if [[ "${{ inputs.channel }}" == "nightly" ]]; then
            python3 .github/scripts/release_channels.py parse-tag \
              --component lume \
              --channel nightly \
              --tag "nightly-lume-v${VERSION}" >/dev/null
          else
            SOURCE_VERSION=$(tr -d '[:space:]' < libs/lume/VERSION)
            if [[ "$SOURCE_VERSION" != "$VERSION" ]]; then
              echo "Tag/input version $VERSION does not match libs/lume/VERSION ($SOURCE_VERSION)"
              exit 1
            fi
            python3 .github/scripts/validate_release_versions.py --product lume
          fi

          # Set output for later steps
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Stage nightly artifact version
        if: inputs.channel == 'nightly'
        run: python3 .github/scripts/release_channels.py apply-version --component lume --version "${{ inputs.version }}"

      - name: Select Xcode 16.3
        run: |
          sudo xcode-select -s /Applications/Xcode_16.3.app
          xcodebuild -version

      - name: Install dependencies
        run: |
          brew install cpio

      - name: Create .release directory
        run: mkdir -p .release

      - name: Import Certificates
        env:
          APPLICATION_CERT_BASE64: ${{ secrets.APPLICATION_CERT_BASE64 }}
          INSTALLER_CERT_BASE64: ${{ secrets.INSTALLER_CERT_BASE64 }}
          CERT_PASSWORD: ${{ secrets.CERT_PASSWORD }}
          KEYCHAIN_PASSWORD: "temp_password"
        run: |
          # Create a temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
          security set-keychain-settings -t 3600 -l build.keychain

          # Import certificates
          echo $APPLICATION_CERT_BASE64 | base64 --decode > application.p12
          echo $INSTALLER_CERT_BASE64 | base64 --decode > installer.p12

          # Import certificates silently (minimize output)
          security import application.p12 -k build.keychain -P "$CERT_PASSWORD" -T /usr/bin/codesign -T /usr/bin/pkgbuild > /dev/null 2>&1
          security import installer.p12 -k build.keychain -P "$CERT_PASSWORD" -T /usr/bin/codesign -T /usr/bin/pkgbuild > /dev/null 2>&1

          # Allow codesign to access the certificates (minimal output)
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$KEYCHAIN_PASSWORD" build.keychain > /dev/null 2>&1

          # Verify certificates were imported
          echo "Verifying signing identities..."
          CERT_COUNT=$(security find-identity -v -p codesigning build.keychain | grep -c "Developer ID Application" || echo "0")
          INSTALLER_COUNT=$(security find-identity -v build.keychain | grep -c "Developer ID Installer" || echo "0")

          if [ "$CERT_COUNT" -eq 0 ]; then
            echo "Error: No Developer ID Application certificate found"
            security find-identity -v -p codesigning build.keychain
            exit 1
          fi

          if [ "$INSTALLER_COUNT" -eq 0 ]; then
            echo "Error: No Developer ID Installer certificate found"  
            security find-identity -v build.keychain
            exit 1
          fi

          echo "Found $CERT_COUNT Developer ID Application certificate(s) and $INSTALLER_COUNT Developer ID Installer certificate(s)"
          echo "All required certificates verified successfully"

          # Clean up certificate files
          rm application.p12 installer.p12

      - name: Install Provisioning Profile
        env:
          PROVISIONING_PROFILE_BASE64: ${{ secrets.PROVISIONING_PROFILE_BASE64 }}
        run: |
          echo "Installing provisioning profile..."
          echo "$PROVISIONING_PROFILE_BASE64" | base64 --decode > libs/lume/resources/embedded.provisionprofile
          echo "Provisioning profile installed successfully"

      - name: Build and Notarize
        id: build_notarize
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          TEAM_ID: ${{ secrets.TEAM_ID }}
          APP_SPECIFIC_PASSWORD: ${{ secrets.APP_SPECIFIC_PASSWORD }}
          # These will now reference the imported certificates
          CERT_APPLICATION_NAME: "Developer ID Application: ${{ secrets.DEVELOPER_NAME }} (${{ secrets.TEAM_ID }})"
          CERT_INSTALLER_NAME: "Developer ID Installer: ${{ secrets.DEVELOPER_NAME }} (${{ secrets.TEAM_ID }})"
          VERSION: ${{ steps.set_version.outputs.version }}
          BUNDLE_VERSION: ${{ inputs.bundle_version || steps.set_version.outputs.version }}
        working-directory: ./libs/lume
        run: |
          # Minimal debug information
          echo "Starting build process..."
          echo "Swift version: $(swift --version | head -n 1)"
          echo "Building version: $VERSION"

          # Ensure .release directory exists
          mkdir -p .release
          chmod 755 .release

          # Build the project first (redirect verbose output)
          echo "Building project..."
          swift build --configuration release > build.log 2>&1
          echo "Build completed."

          # Run the notarization script with LOG_LEVEL env var
          chmod +x scripts/build/build-release-notarized.sh
          cd scripts/build
          LOG_LEVEL=minimal ./build-release-notarized.sh

          # Return to the lume directory
          cd ../..

          # Debug: List what files were actually created
          echo "Files in .release directory:"
          find .release -type f -name "*.tar.gz" -o -name "*.pkg.tar.gz"

          # Get architecture for output filename
          ARCH=$(uname -m)
          OS_IDENTIFIER="darwin-${ARCH}"

          # Output paths for later use
          echo "tarball_path=.release/lume-${VERSION}-${OS_IDENTIFIER}.tar.gz" >> $GITHUB_OUTPUT
          echo "pkg_path=.release/lume-${VERSION}-${OS_IDENTIFIER}.pkg.tar.gz" >> $GITHUB_OUTPUT

      - name: Upload build log on failure
        if: failure() && steps.build_notarize.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: swift-build-log
          path: ./libs/lume/build.log
          retention-days: 7

      - name: Create Standard Version Releases
        working-directory: ./libs/lume/.release
        run: |
          VERSION=${{ steps.set_version.outputs.version }}
          ARCH=$(uname -m)
          OS_IDENTIFIER="darwin-${ARCH}"

          # Create OS-tagged symlinks
          ln -sf "lume-${VERSION}-${OS_IDENTIFIER}.tar.gz" "lume-darwin.tar.gz"
          ln -sf "lume-${VERSION}-${OS_IDENTIFIER}.pkg.tar.gz" "lume-darwin.pkg.tar.gz"

          # Create simple symlinks
          ln -sf "lume-${VERSION}-${OS_IDENTIFIER}.tar.gz" "lume.tar.gz"
          ln -sf "lume-${VERSION}-${OS_IDENTIFIER}.pkg.tar.gz" "lume.pkg.tar.gz"

          # List all files (including symlinks)
          echo "Files with symlinks in release directory:"
          ls -la

      - name: Stage notarized release assets
        working-directory: ./libs/lume/.release
        run: |
          mkdir -p release-upload
          for asset in lume-*.tar.gz lume-darwin.tar.gz lume-darwin.pkg.tar.gz lume.tar.gz lume.pkg.tar.gz; do
            [ -e "$asset" ] || continue
            cp -L "$asset" "release-upload/$asset"
          done
          ls -lh release-upload

      - name: Upload notarized release assets
        uses: actions/upload-artifact@v4
        with:
          name: lume-release-assets
          path: ./libs/lume/.release/release-upload/
          if-no-files-found: error

  release:
    needs: notarize
    if: startsWith(github.ref, 'refs/tags/lume-v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download notarized release assets
        uses: actions/download-artifact@v4
        with:
          name: lume-release-assets
          path: release-upload

      - name: Verify checked-in release versions
        run: python3 .github/scripts/validate_release_versions.py --product lume

      - name: Collect PR-first release attribution
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mkdir -p release-metadata
          python3 .github/scripts/release_attribution.py collect \
            --repository "${{ github.repository }}" \
            --product lume \
            --display-name Lume \
            --version "${{ needs.notarize.outputs.version }}" \
            --tag "${{ github.ref_name }}" \
            --tag-prefix lume-v \
            --sha "${{ github.sha }}" \
            --path libs/lume \
            --changelog libs/lume/CHANGELOG.md \
            --asset-dir release-upload \
            --output release-metadata/release-manifest.json

      - name: Render release body and announcement draft
        run: |
          python3 .github/scripts/release_attribution.py render \
            --manifest release-metadata/release-manifest.json \
            --body release-metadata/release-body.md \
            --social release-metadata/release-social.txt \
            --card release-metadata/release-card.svg \
            --alt release-metadata/release-card-alt.txt \
            --footer .github/release-notes/lume.md
          cp release-metadata/release-manifest.json release-upload/
          if [ -f release-metadata/release-card.svg ]; then
            sudo apt-get update
            sudo apt-get install -y --no-install-recommends librsvg2-bin
            rsvg-convert release-metadata/release-card.svg \
              --output release-metadata/release-card.png
            cp release-metadata/release-card.png release-upload/
          fi

      - name: Preserve release evidence
        uses: actions/upload-artifact@v4
        with:
          name: lume-release-metadata-${{ needs.notarize.outputs.version }}
          path: release-metadata/
          if-no-files-found: error
          retention-days: 90

      - name: Publish the verified Release Please draft
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 .github/scripts/github_release.py \
            --repository "${{ github.repository }}" \
            --tag "${{ github.ref_name }}" \
            --sha "${{ github.sha }}" \
            --body release-metadata/release-body.md \
            --asset-dir release-upload
