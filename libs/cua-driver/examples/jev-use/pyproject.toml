[project]
name = "cua-driver-jev-use-example"
version = "0.1.0"
description = "Mockable jev-use decision loop with the TypeSafe Jev provider and Cua Driver"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.26.0,<2",
  "typesafe-sdk>=0.6.0,<1",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = []
